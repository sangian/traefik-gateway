---
# Dynamic Traefik Middlewares
# Rate limiting middlewares are defined in docker-compose.yml labels
# (environment variable substitution doesn't work in file provider)
#
# Additional static middlewares can be defined here and referenced with @file
# Example: traefik.http.routers.myrouter.middlewares=my-middleware@file

http:
  middlewares:
    # Example: Custom middleware for specific use cases
    # Add your static middlewares here if needed



